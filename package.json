{
  "name": "ht-lens-jstests",
  "version": "0.0.0",
  "private": true,
  "description": "JS test deps for ht_lens (jsdom-based viewer tests run under Node in CI).",
  "devDependencies": {
    "jsdom": "25.0.1"
  }
}
