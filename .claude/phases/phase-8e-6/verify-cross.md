## 1. Verification of automated checks

The verify report is not stale with respect to committed code: HEAD is `f23b2d9` verify, after code commits `34725af` and `50ba280`, and no later committed source changes are visible. However, the “tracking tree clean” wording is weaker than `WORKFLOW.md`’s required `git status` clean: current status has untracked `.claude/phases/phase-8e-6/summary.md`, `.claude/phases/phase-8e-6/verify-cross.md`, `.claude/scheduled_tasks.lock`, `.ipynb_checkpoints/`, `logs/`, and `repair_seeds/book.detected.json`.

The lint/format/type/test evidence is plausible, but I cannot treat it as fully clean because `repair_seeds/book.detected.json` appears to be generated by the new default CLI path. `tests/integration/test_repair_cli.py:267-287` invokes `detect-repairs` without `--out`, and `src/ht_lens/cli.py:603-609` writes to `repair_seeds/<stem>.detected.json` relative to repo CWD. That means the full test or live verify path can dirty the repo.

Coverage is not separately evidenced. The self table says “coverage” is included in tests, but no coverage command/result is shown. CI is correctly marked pending push, so the −2 stability deduction is justified.

## 2. Verification of functional checks

The narrowed design from debate was mostly followed: no ingest hook, no migration, no second manifest, and `repair-images` remains the only `overrides.json` writer. That directly addresses the debate’s largest over-engineering concerns.

Functional coverage is incomplete against the revised challenge checklist. `.claude/phases/phase-8e-6/challenge.md:51-54` calls for doc1 degraded 3/3, 5-doc FP 0, and doc5 four-page caption mispair re-detection. Verify only gives live doc1 degraded and one caption-mispair page; doc5 is checked only for existing manifest serving, not for `detect-repairs` re-detection.

The “collision-free `p<page>_o<order>_`” claim is not adequately exercised. The only CLI detector test has one degraded image basename (`tests/integration/test_repair_cli.py:238-258`), so it does not cover same-page duplicate basenames, the exact debate edge case.

The “read-only” claim should be read as “no DB/overrides writes,” not filesystem read-only. The CLI intentionally writes previews and a draft seed, and the default draft seed path currently lands in `repair_seeds/`.

## 3. Score audit

독창성 13/15: Mostly justified. The seed-centered audit CLI is a good simplification versus the original candidates-manifest plan. Deduct 1 more because the default output behavior writes into the project’s durable seed directory, which weakens the clean source-of-truth lifecycle.

완결성 33/35: Too high. Revised DoD evidence for doc5 caption re-detection and 5-doc FP 0 is asserted but not shown in tests or verify evidence. The duplicate-basename preview collision guard from debate/challenge is also not actually locked. I would score 29-30/35.

안정성 28/30: Too high. Automated checks likely pass, but the test/CLI path can dirty the repo, and duplicate basename handling loses candidate identity. `src/ht_lens/cli.py:549` maps basename to one `order_idx`, then `src/ht_lens/cli.py:556-562` emits preview names and allowlist entries from basename only. That is fragile under repeated basenames. I would score 24-25/30.

확장성 19/20: Slightly optimistic. The approach is extensible, but candidate identity is still basename-centered while the phase is explicitly for future larger docs/book2. `detect_degraded_images` returns only page/basename/bbox (`src/ht_lens/image_repair.py:154-172`), so the CLI has to reconstruct order identity lossy. I would score 17/20.

## 4. Issues missed (new this round)

The duplicate-basename collision fix is not real. `order_by_base = {os.path.basename(c.img_path or ""): c.order_idx for c in rows}` at `src/ht_lens/cli.py:549` collapses all chunks with the same basename to the last order. Two degraded same-page images named `image.jpg` will both target the same `pXXXX_oYYYY_image.png` at `src/ht_lens/cli.py:557-560`, and `image_allowlist` at `src/ht_lens/cli.py:562` cannot distinguish them. This directly misses challenge R6 and debate’s same-basename test.

The test suite or verify flow leaves a generated draft seed in the repo. `repair_seeds/book.detected.json` currently contains tmp paths from `pytest-of-hyunlord`, and `git status` shows it untracked. This is not just cosmetic: a future run can overwrite `repair_seeds/<filename>.detected.json` because `src/ht_lens/cli.py:609` uses `write_text` unconditionally.

Skip reporting lacks stable evidence. `_skipped` entries only include `basename` and `reason` at `src/ht_lens/cli.py:563-565`. For duplicate basenames, multi-page repeats, or review handoff, the report cannot identify which page/order/bbox failed clipping. That undercuts the “no silent skip” claim.

Caption detector tests are broad but not live against the actual F1 defect set. Unit tests cover synthetic captionless+captioned cases (`tests/unit/test_image_repair.py:402-441`), but the revised checklist specifically required doc5 four-page re-detection. Verify only confirms doc5 existing override serving, not that the new detector would find those pages.

## 5. Verdict

**DOWNGRADE** — The implementation is directionally sound and substantially addresses the debate by avoiding ingest/schema/serving writes, so I would not reject. But the self-score should come down to about **88-90**: duplicate-basename preview/allowlist identity is still broken and untested, the new CLI/test path dirties the repo with a draft seed, and the doc5 caption re-detection evidence promised by the revised DoD is missing.
